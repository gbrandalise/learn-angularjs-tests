# learn-angularjs-tests

## Comandos

`npm init`

`npm install angular`

`npm install karma jasmine phantomjs angular-mocks karma-babel-preprocessor @babel/core @babel/preset-env --save-dev`

`npm install -g karma-cli`

`karma init`